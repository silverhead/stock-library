{{ form_start(form, {'attr': {'class': 'w-100', 'action': path('product_search_result')}}) }}
    {{ form_widget(form.search, {
        'attr':
        {
            'class': 'form-control form-control-dark w-100',
            'placeholder': 'Rechercher un produit',
            'aria-label': 'Rechercher un produit'
        }
    } )}}
{{ form_end(form) }}