{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            {% set backLink =  path('category_index') %}
            {% if parent is not null %}
                {% set backLink =  path('category_sub_list', {id: parent}) %}
            {% endif %}
            <h1><a href="{{ backLink }}" class="btn btn-light">Retour</a> Nouvelle catégorie</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            {{ include('category/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
    </div>
{% endblock %}
